# superintent CLI Prerequisites

## Requirements

- Node.js 18+ and npm

## Run CLI

```bash
npx superintent@latest
```

## Verify CLI

```bash
npx superintent status
```

If this works, return to `/superintent:setup` for project configuration.
